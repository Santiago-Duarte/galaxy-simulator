public interface Gravitacional {

    // Atrae a otro cuerpo celeste e imprime el efecto
    void atraer(CuerpoCeleste otro);

    // Calcula e imprime la fuerza de atraccion segun la distancia real entre cuerpos
    void fuerza_atraccion(int distancia);
}